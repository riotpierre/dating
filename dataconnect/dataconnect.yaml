specVersion: "v1beta"
serviceId: "Ann koutize"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Ann koutize-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
